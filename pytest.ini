[pytest]
testpaths = tests
python_files = test_*.py
pythonpath = .
addopts = -q
# Veraltete SQLAlchemy-Aufrufe sollen auffallen, solange sie noch
# funktionieren - nicht erst, wenn eine neue Version sie entfernt.
filterwarnings =
    error::sqlalchemy.exc.LegacyAPIWarning
